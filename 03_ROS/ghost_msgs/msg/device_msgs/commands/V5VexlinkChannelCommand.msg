ghost_msgs/V5DeviceHeader device_header

float32 tx_x      # this robot's x position to broadcast (meters, map frame)
float32 tx_y      # this robot's y position to broadcast (meters, map frame)
float32 tx_theta  # this robot's heading to broadcast (radians, map frame, CCW positive)
