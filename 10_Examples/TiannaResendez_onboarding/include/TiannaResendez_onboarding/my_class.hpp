#pragma once // Prevents the file from being included multiple times

#include <iostream> // Needed for std::cout

// We are defining a class called MyClass
class MyClass {
public:
  // This is a "constructor", it runs when you create a MyClass object
  MyClass();

  // This is a function the class can perform
  void say_hello();
};
